{
  "name": "pers-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "@tailwindcss/typography": "^0.5.19",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^7.3.2",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.3"
  },
  "_comment_overrides": "Keeps a single copy of vite shared by astro and @tailwindcss/vite. Must satisfy astro's own vite range (astro 7 requires ^8), or `astro build` fails with 'rollupOptions.input should not be an html file'.",
  "overrides": {
    "vite": "^8.0.13"
  }
}
